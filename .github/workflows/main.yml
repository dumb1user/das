name: ci

on:
  push:
    branches:
      - 'master'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v2
      -
        name: Log in to YC
        uses: docker/login-action@v1
        with:
          registry: cr.yandex
          username: oauth
          password: AQAAAAA0Nm7zAATuwRYE2Y4geEtzhLbafnw6d4M
      -
         name: Build, tag, and push image to Yandex Cloud Container Registry
         run: |
          docker build -t cr.yandex/crpqi4tdcdpvjegk0nu4/time:latest .
          docker push cr.yandex/crpqi4tdcdpvjegk0nu4/time:lates

